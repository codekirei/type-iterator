{
  "name": "type-iterator",
  "version": "0.1.0",
  "description": "run a callback function against specific js types",
  "main": "index.js",
  "repository": "codekirei/type-iterator",
  "scripts": {
    "test": "mocha",
    "test-ci": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly",
    "cover": "istanbul cover node_modules/mocha/bin/_mocha &> /dev/null && istanbul report text && rm -r ./coverage",
    "dev": "find index.js test/test.js | entr -r mocha -R min"
  },
  "keywords": [
    "type",
    "types",
    "iterator",
    "iteration"
  ],
  "author": "Jacob Blakely (codekirei)",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.4.0",
    "istanbul": "^0.4.0",
    "lodash.clonedeep": "^3.0.2",
    "mocha": "^2.3.3",
    "sinon": "^1.17.2"
  }
}
